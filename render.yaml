services:
  - type: web
    name: gridiron24
    runtime: node
    # Starter required for persistent disk (accounts, news, calendar, rankings).
    plan: starter
    region: ohio
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /api/health
    disk:
      name: gridiron24-data
      mountPath: /var/data
      sizeGB: 1
    envVars:
      - key: NODE_VERSION
        value: "20"
      - key: DATA_DIR
        value: "/var/data"
      - key: COMMISSIONER_LOGIN
        value: "sevans"
      - key: COMMISSIONER_EMAIL
        value: "sevans5714@gmail.com"
      - key: APP_BASE_URL
        value: "https://www.gridiron24.com"
      - key: COMMISSIONER_PASSWORD
        sync: false
      - key: SESSION_SECRET
        sync: false
      - key: RESEND_API_KEY
        sync: false
      - key: MAIL_FROM
        sync: false
      - key: CRON_SECRET
        sync: false
      - key: OPENAI_API_KEY
        sync: false
      - key: ANTHROPIC_API_KEY
        sync: false
      - key: WRAP_EMAIL_MODE
        value: "all"

  - type: cron
    name: gridiron24-weekly-wrap
    runtime: node
    plan: starter
    region: ohio
    schedule: "0 14 * * 2"
    buildCommand: npm install
    startCommand: node -e "const u=process.env.APP_BASE_URL||'https://www.gridiron24.com';const s=process.env.CRON_SECRET;if(!s){console.error('CRON_SECRET missing');process.exit(1);}fetch(u.replace(/\\/$/,'')+'/api/cron/weekly-wrap',{method:'POST',headers:{Authorization:'Bearer '+s}}).then(async r=>{const t=await r.text();console.log(r.status,t.slice(0,500));if(!r.ok)process.exit(1);}).catch(e=>{console.error(e);process.exit(1);});"
    envVars:
      - key: NODE_VERSION
        value: "20"
      - key: APP_BASE_URL
        value: "https://www.gridiron24.com"
      - key: CRON_SECRET
        sync: false

  - type: cron
    name: gridiron24-rules-sync
    runtime: node
    plan: starter
    region: ohio
    # Mondays and Thursdays at 14:00 UTC (~10am ET)
    schedule: "0 14 * * 1,4"
    buildCommand: npm install
    startCommand: node -e "const u=process.env.APP_BASE_URL||'https://www.gridiron24.com';const s=process.env.CRON_SECRET;if(!s){console.error('CRON_SECRET missing');process.exit(1);}fetch(u.replace(/\\/$/,'')+'/api/cron/rules-sync',{method:'POST',headers:{Authorization:'Bearer '+s}}).then(async r=>{const t=await r.text();console.log(r.status,t.slice(0,800));if(!r.ok)process.exit(1);}).catch(e=>{console.error(e);process.exit(1);});"
    envVars:
      - key: NODE_VERSION
        value: "20"
      - key: APP_BASE_URL
        value: "https://www.gridiron24.com"
      - key: CRON_SECRET
        sync: false

  - type: cron
    name: gridiron24-roster-violations
    runtime: node
    plan: starter
    region: ohio
    # Daily 14:00 UTC (~10am ET) — scan injured starters and warn managers
    schedule: "0 14 * * *"
    buildCommand: npm install
    startCommand: node -e "const u=process.env.APP_BASE_URL||'https://www.gridiron24.com';const s=process.env.CRON_SECRET;if(!s){console.error('CRON_SECRET missing');process.exit(1);}fetch(u.replace(/\\/$/,'')+'/api/cron/roster-violations',{method:'POST',headers:{Authorization:'Bearer '+s}}).then(async r=>{const t=await r.text();console.log(r.status,t.slice(0,800));if(!r.ok)process.exit(1);}).catch(e=>{console.error(e);process.exit(1);});"
    envVars:
      - key: NODE_VERSION
        value: "20"
      - key: APP_BASE_URL
        value: "https://www.gridiron24.com"
      - key: CRON_SECRET
        sync: false
